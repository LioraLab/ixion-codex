---
name: ixion-pipeline-phase-3-mockup
description: (Pipeline Phase 3) 프레임워크 없이 HTML/CSS/JS + JSON으로 빠르게 목업을 만들어 UX/흐름을 검증한다.
metadata:
  short-description: Pipeline phase 3 - mockup
---

# Pipeline Phase 3: Mockup

## 목표
- 구현 전에 UX/흐름을 “작동하는 프로토타입”으로 검증한다.
- 나중에 프레임워크(Next.js 등)로 옮기기 쉽게 컴포넌트 단위로 생각한다.

## 원문 참고(SSOT)
- bkit phase-3-mockup: `.codex/.ixion/plugins/bkit/skills/phase-3-mockup/SKILL.md`
- 템플릿: `.codex/.ixion/templates/pipeline/phase-3-mockup.template.md`

## 권장 산출물(예시)
- `mockup/` (pages/styles/scripts/data)
- `.codex/.ixion/docs/02-design/mockup-spec.md`

## 체크리스트(최소)
- 핵심 유저 여정 1-3개(로그인, 결제, 온보딩 등)만 먼저
- 데이터는 JSON으로 시뮬레이션(API 없이)
- 컴포넌트 후보(Button/Card/Form 등) 식별
- 접근성/모바일 터치 타겟/로딩/에러/빈 상태를 목업에 반영

## 다음
- Phase 4(API): `$ixion-pipeline-phase-4-api`

