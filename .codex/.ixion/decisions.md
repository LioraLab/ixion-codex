# Decisions

이 파일은 익션(automations)으로 진행한 작업에서 "중요한 결정"을 짧게 기록하는 로그다.

규칙
- 5-10줄로 짧게
- 무엇을, 왜, 대안은 무엇이었는지
- 영향 범위와 롤백 포인트(있으면)

템플릿
- 날짜:
- 주제:
- 결정:
- 이유:
- 대안:
- 영향/리스크:

---

- 날짜: 2026-02-14
- 주제: NotebookLM 프로젝트 지식 베이스 생성
- 결정: NotebookLM에 `ixion-codex` 노트북을 만들고(Notebook ID: `c50d8a9e-cdc6-44df-8e38-337aca318165`), ixion-codex 관련 지식을 앞으로 여기로 축적한다.
- 이유: 레포/익션 지식을 검색/요약/재사용하기 위한 SSOT 보조 지식베이스가 필요함.
- 대안: `Assistant Memory`에만 축적(개인 기억과 프로젝트 지식이 섞여 관리가 어려움).
- 영향/리스크: 노트북과 레포 상태가 어긋날 수 있으므로, 스킬/문서 변경 시 노트북 소스도 함께 최신화한다.

---

- 날짜: 2026-02-14
- 주제: 스킬 수 축소(병합/정리)
- 결정:
  - 스킬 표면적을 91개 → 35개로 축소했다.
  - 패턴 스킬을 `$ixion-patterns`로 통합(API/DB/Postgres/Docker/배포).
  - bkend 스킬을 `$ixion-bkend`로 통합(연결/MCP/인증/데이터/스토리지/예제).
  - pipeline phase 스킬(1~9)은 `$ixion-development-pipeline` 안으로 병합(Phase별 체크리스트 포함).
  - 에이전트 엔트리포인트는 `$ixion-agent-executor` 1개만 유지(옵트인).
- 이유: 사용자가 “어떤 스킬을 골라야 하는지” 인지 부하가 커져 실행 속도가 느려짐.
- 대안: 기존 스킬을 유지하고 문서만 보강(`SKILL-MAP.md`)하되 목록은 그대로 둠(근본적으로 개수 문제는 남음).
- 영향/리스크: 기존에 삭제된 스킬명을 직접 호출하던 워크플로우가 깨질 수 있음(대체 스킬로 업데이트 필요).

---

- 날짜: 2026-02-14
- 주제: 스킬 수 추가 축소(프리셋/도메인 통합)
- 결정:
  - 스킬 표면적을 35개 → 29개로 추가 축소했다.
  - 레벨 프리셋 3개(`starter/dynamic/enterprise`)를 `$ixion-presets`로 통합했다.
  - 도메인 프리셋 5개(`web/app/software/cv/shopify`)를 `$ixion-domains`로 통합했다.
  - 기존 스킬 디렉토리는 삭제 대신 `.codex/.ixion/archives/skills/`로 이동해 “참고용 아카이브”로 보관한다.
- 이유: “스킬 선택” 자체가 병목이 되지 않게, 시작점을 더 적게 유지한다.
- 영향/리스크: 과거 프롬프트/노트/문서에서 삭제된 스킬명을 호출하면 실패할 수 있으므로, `$ixion-prompt`를 통해 새 엔트리포인트로 갱신한다.

---

- 날짜: 2026-02-14
- 주제: 스킬 수 추가 축소(Plan/Deep Search/Playwright 통합)
- 결정:
  - 스킬 표면적을 29개 → 26개로 추가 축소했다.
  - `$ixion-plan`을 제거하고, `$ixion-pdca`의 “Plan 작성 모드”로 흡수했다.
  - `$ixion-deepsearch`를 제거하고, `$ixion-analyze`의 “Deep Search 모드”로 흡수했다.
  - `$ixion-e2e-testing`을 제거하고, `$ixion-playwright`로 통합했다.
  - `$ixion-playwright-cli`는 `$ixion-playwright`로 rename(기능 범위 확장)했다.
- 이유: 사용자가 “뭘 골라야 하지?”를 덜 고민하게 하고, Playwright 관련 기능을 1곳으로 모아 재현/테스트/디버깅 흐름을 단순화한다.
- 대안: 각 스킬을 유지하고 문서(`SKILL-MAP.md`)만 더 보강(스킬 목록 자체가 길다는 문제는 남음).
- 영향/리스크: 과거 프롬프트/문서에서 제거된 스킬명을 호출하면 실패할 수 있으므로, `.codex/.ixion/archives/skills/README.md`에 대체 관계를 유지하고 주요 문서를 동기화한다.

---

- 날짜: 2026-02-14
- 주제: 검증 게이트(상황별, 품질 우선)
- 결정:
  - 코드/설정 변경이면 검증 2개를 기본값으로 실행한다(가능한 한 빠른 2개, 없으면 1개).
  - 읽기 전용 분석/문서 작업이면 검증은 0-1개(필요할 때만)로 둔다.
- 이유: 품질을 최우선으로 두되, 불필요한 검증으로 속도를 무의미하게 깎지 않기 위함.
- 대안: 항상 2개 강제(문서/탐색 작업에서도 불필요한 비용이 발생).
- 영향/리스크: 프로젝트마다 가능한 검증 커맨드가 다르므로, 검증 경로가 불명확하면 `$ixion-doctor`로 먼저 후보를 찾는다.
